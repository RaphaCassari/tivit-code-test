service: fastapi-lambda

provider:
  name: aws
  runtime: python3.9

functions:
  app:
    handler: main.handler
    events:
      - http:
          path: /sync-user
          method: get

plugins:
  - serverless-python-requirements

custom:
  pythonRequirements:
    dockerizePip: true
